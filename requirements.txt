# --- Web framework ---
Flask==3.0.3
Flask-Cors==4.0.1
# --- Auth & security ---
PyJWT==2.9.0
bcrypt==4.2.0
# --- Database ---
mysql-connector-python==9.0.0
# --- Config ---
python-dotenv==1.0.1
# --- Async processing ---
celery==5.4.0
redis==5.0.8
# --- Observability ---
opentelemetry-api==1.27.0
opentelemetry-sdk==1.27.0
# --- LLM provider ---
google-genai==2.17.0
# --- HTTP (ALM/tool adapters) ---
requests==2.32.3
# --- Testing ---
pytest==8.3.3
pytest-flask==1.3.0

# RAG stack (optional at runtime — imported lazily; see app/rag).
# Uncomment to enable real embeddings/vector store:
# chromadb==0.5.5
# sentence-transformers==3.1.1
# docling==2.5.2
